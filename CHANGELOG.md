# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 1.6.1

### 修复

- **忙率虚高时不再乱限速**：连续 3 秒忙率 ≥90% 但该磁盘读写吞吐 ≈0、队列 ≈0（SSD 自身回收 / TRIM / 计数器异常）时，
  判定为"限速没有意义"：跳过本轮，并把已施加的自动限速还原，同时写明原因。
  （实测日志里因此避免了去限速 msedgewebview2 / wslservice / 前台 codex 这类只是"IO 变慢"的进程，它们被限速后磁盘忙率没有任何改善。）
- **紧急限速分级挑目标**：第一级要求真的有 IO 量（≥512 KB/s 或 ≥4 次/秒），第二级才放宽到"低传输但高占用"，
  第三级才让"保护前台"让位；低传输的元凶仍然能被选中，但不会再误伤只是 IO 变慢的无关进程。
- **磁盘编号兜底错位**：某次 PDH 采样里缺少目标磁盘时，旧代码会退回到"列表里的第一块磁盘"，
  于是"监控磁盘 0"可能实际读到的是磁盘 1 的忙率（实测日志里出现过"磁盘 0 忙率 100%"而磁盘 0 其实空闲）。
  现在取不到目标磁盘就返回空值（该秒不触发任何限速），并且限速/无候选日志都会附带**实际采样的实例名与读写速率**，
  方便核对"忙率"到底来自哪块盘。
- **Windows「优化驱动器」(defrag.exe) 提示**：检测到它正在运行而本工具又无从下手（无候选 / 无实际吞吐）时，
  日志会直接给出"在优化驱动器里取消勾选该磁盘"的解决办法——这是本机反复出现长时间全系统卡顿的根源。

### 变更

- 自检命令 `throttle` 新增 `--iops N`（默认 800），可以手动复现/验证 IOPS 上限；
  `load --random --path X:` 可在指定磁盘上生成 4K 随机写（FUA）负载，用于模拟聊天软件数据库这类小 IO。

## 1.6.0

### 修复

- **低传输、高占用的进程限不住**：二级限速以前只限吞吐（MB/s），而 4K 随机等小 IO 场景下
  进程可能只有几百 KB/s 却把磁盘占满，只限 MB/s 完全拦不住。现在**同时限制每秒 IO 次数（IOPS）**，
  Windows 存储 QoS 会按先到的那个上限执行：
  - 带宽默认 30 MB/s，IOPS 上限按目标当前 IO 次数自适应（约 2/3，最多 800、最低 8 次/秒），
    仍不达标就每 5 秒同时收紧（30 → 15 → … 最低 2 MB/s；IOPS 继续对折，最低 8 次/秒）；
  - 紧急保护直接按同一策略施加上限（实测卡顿里微信只有 1.4 MB/s、每秒几十次 IO 就把 USB 外置盘占满，
    旧的 30 MB/s 上限等于没限）；
  - 状态卡与日志都会显示当前的 IOPS 上限。
- **外壳/输入进程被限速，表现为"打不开程序、打不了字"**：`explorer`、`sihost`、`userinit`、
  `taskhostw`、`StartMenuExperienceHost`、`ShellExperienceHost`、`SearchHost`、`TextInputHost`、`ctfmon`
  等桌面/开始菜单/搜索/输入法进程加入硬保护，任何模式下都不再限速。
- **紧急满载时反复"解除 → 2 秒后再限速"**：被限速进程切到前台时，旧逻辑会立即解除限速，
  而磁盘仍是 100% 忙，2 秒后又只能重新限速（实测一次卡顿里连续抖动 4 次）。
  现在忙率 ≥ 紧急阈值期间保持限速，等磁盘回落再正常收尾。
- **紧急保护不再漏掉低传输元凶**：紧急模式不再套用"最低速率 512 KB/s / 最低 IO 次数"门槛，
  低传输但高占用的进程也能被选中（1.6.1 起改为分级挑选，见上）。

### 变更

- 监控线程（引擎采样、ETW 事件处理）降到"低于正常"线程优先级：磁盘已经满载时不和前台程序抢 CPU。

## 1.5.0

### 新增

- **首次启动会问一次界面语言**：还没有配置文件时（全新安装、或删除配置后首次运行）弹出语言选择框，
  默认按系统显示语言预选（读不到或不在支持列表内则英语），改选项时对话框本身立刻按所选语言预览；
  - 点「确定」= 记住这次选择（写进配置，之后不再看系统语言）；
  - 点「稍后再说」或直接关闭 = 保持「跟随系统语言」，之后可在「设置 → 界面语言」里随时更改。
- 语言框在从终端 / 计划任务等没有前台权限的进程启动时，会主动把自己顶到最前一次，避免被别的窗口挡住看不见。

### 变更

- 字体与排版方向（阿拉伯语从右往左）相关逻辑抽到 `UiLanguage`，主窗口与首次启动语言框共用。

## 1.4.1

### 修复

- **读不到系统显示语言时会自我递归**：首次运行的语言探测里，空语言名会被当成“跟随系统”再次进入探测，
  极端情况下（系统未设置显示语言 / 不变文化）导致栈溢出。现在这种情况直接按英语兜底，与“非上述语言一律英语”一致。

## 1.4.0

### 新增

- **界面多语言**：设置页新增「界面语言」，支持**简体中文 / 繁體中文 / English / 日本語 / 한국어 / العربية** 六种语言。
  - 选择后**立即生效**并自动保存，不需要重启程序；首次运行按系统显示语言自动选择（未知语言回退英语）。
  - 语言下拉用母语名称显示，看不懂当前语言时也能找到自己的语言。
- **阿拉伯语从右往左排版**：窗口、标签页、表格、菜单与托盘菜单整体镜像，并单独处理了「6.6 MB/s」「12%」这类数字片段在 RTL 下的显示顺序（不会变成「MB/s 6.6」）。
- **按语言切换字体**：雅黑（简中）/ 正黑（繁中）/ Yu Gothic UI（日）/ Malgun Gothic（韩）/ Segoe UI（英、阿），避免中日韩字形串味。
- **文案全覆盖**：界面文本、托盘菜单与提示、窗口提示气泡、弹窗、以及日志页和日志文件里的引擎消息都会跟着语言变化。
- 新增 `DiskGuard.Cli.exe i18n` 自检命令，校验六种语言的词条是否齐全（缺少任意一条即失败）；CI 每次构建都会执行。

### 变更

- 设置页最前面新增「界面语言」卡片；切换语言时会同时刷新磁盘下拉框、日志列表与托盘菜单。

### 修复

- 切换语言后，监控磁盘下拉框里的「磁盘 0 (C: D:)」仍显示旧语言，现在会随语言重建。

## 1.3.0

### 新增

- 界面新增「关于」页：作者署名（by Hison）、抖音 / 小红书账号（可一键复制）、开源致谢与免责声明。
- 发布到 GitHub 后，在 `MainWindow.xaml.cs` 的 `ProjectUrl` 填入仓库地址，「关于」页会自动出现"打开项目主页"按钮。

## 1.2.0

### 变更

- **单文件 exe 体积从 67.5 MB 降到 57.4 MB（约 −15%）**
  - 托盘图标改为直接调用 `Shell_NotifyIcon`，不再引用 WinForms；
  - 自包含发布时按 FrameworkList 的 `Profile` 剔除 WinForms 专属程序集（WPF 用不到）；
  - 只保留 TraceEvent 需要的 `KernelTraceControl.dll`（amd64），去掉 msdia140 / DIA / Relogger 与 x86、arm64 副本；
  - 发布不再附带 PDB。
- 发布参数写进 `DiskGuard.App.csproj`，`dotnet publish -c Release` 即可产出与发行版一致的单文件 exe。

### 修复

- **限速目标状态的线程安全**：界面线程（暂停 / 还原 / 手动限速）与采样线程会并发读写同一个限速目标，
  极端情况下同一个进程句柄会被释放两次；现在统一用 `_stateSync` 串行化。
- **“拒绝访问”缓存不再无限增长**：PID 会被系统回收，原实现只增不减，长期运行会持续占用内存。
- **配置文件写入改为原子替换**：写到一半掉电/被杀不再留下半截 JSON（旧行为会静默回退默认设置）。
- **`schtasks` 调用不再可能死锁或取到未退出进程的退出码**：两个输出流改为异步读取并检查等待超时。
- **托盘不可用时关闭窗口会直接退出**：以前会隐藏窗口，而程序再也无法从托盘唤回。
- 日志写入失败只会提示一次，不再反复报错。
- 界面列表排序时对“索引为 -1”的情况做了保护，避免偶发的界面刷新异常。
- 注销 / 关机时也会还原限速（新增 `SessionEnding` 处理）。
- “气泡提示”设置现在对最小化提示与关闭提示都生效（以前只有设置项被忽略）。
- 托盘消息处理全过程加异常兜底：窗口过程抛异常会直接结束进程，现在只降级并记日志。

### 性能

- PID→进程名映射改为 2 秒缓存 + 未命中单独查询，避免 ETW 模式下每秒枚举全部进程。
- 前台程序进程族计算由 O(n²) 改为一次建索引，每秒节省上万次比较。
- 每轮采样不再重复分配集合/LINQ 临时对象（速率历史清理、ETW 窗口均值、DiskStatus 复用）。
- 磁盘下拉框复用 `DiskStatus` 实例，切换磁盘时选中项不再每秒失效。

## 1.1.1

- 首个公开版本：PDH 忙率 + ETW 每进程磁盘占用统计、分级限速（优先级 / 吞吐上限 / 间歇挂起）、托盘常驻、开机自启。
