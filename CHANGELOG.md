# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 1.2.0

### 变更

- **单文件 exe 体积从 67.5 MB 降到 57.4 MB（约 −15%）**
  - 托盘图标改为直接调用 `Shell_NotifyIcon`，不再引用 WinForms；
  - 自包含发布时按 FrameworkList 的 `Profile` 剔除 WinForms 专属程序集（WPF 用不到）；
  - 只保留 TraceEvent 需要的 `KernelTraceControl.dll`（amd64），去掉 msdia140 / DIA / Relogger 与 x86、arm64 副本；
  - 发布不再附带 PDB。
- 发布参数写进 `DiskGuard.App.csproj`，`dotnet publish -c Release` 即可产出与发行版一致的单文件 exe。

### 修复

- **限速目标状态的线程安全**：界面线程（暂停 / 还原 / 手动限速）与采样线程会并发读写同一个限速目标，
  极端情况下同一个进程句柄会被释放两次；现在统一用 `_stateSync` 串行化。
- **“拒绝访问”缓存不再无限增长**：PID 会被系统回收，原实现只增不减，长期运行会持续占用内存。
- **配置文件写入改为原子替换**：写到一半掉电/被杀不再留下半截 JSON（旧行为会静默回退默认设置）。
- **`schtasks` 调用不再可能死锁或取到未退出进程的退出码**：两个输出流改为异步读取并检查等待超时。
- **托盘不可用时关闭窗口会直接退出**：以前会隐藏窗口，而程序再也无法从托盘唤回。
- 日志写入失败只会提示一次，不再反复报错。
- 界面列表排序时对“索引为 -1”的情况做了保护，避免偶发的界面刷新异常。
- 注销 / 关机时也会还原限速（新增 `SessionEnding` 处理）。
- “气泡提示”设置现在对最小化提示与关闭提示都生效（以前只有设置项被忽略）。
- 托盘消息处理全过程加异常兜底：窗口过程抛异常会直接结束进程，现在只降级并记日志。

### 性能

- PID→进程名映射改为 2 秒缓存 + 未命中单独查询，避免 ETW 模式下每秒枚举全部进程。
- 前台程序进程族计算由 O(n²) 改为一次建索引，每秒节省上万次比较。
- 每轮采样不再重复分配集合/LINQ 临时对象（速率历史清理、ETW 窗口均值、DiskStatus 复用）。
- 磁盘下拉框复用 `DiskStatus` 实例，切换磁盘时选中项不再每秒失效。

## 1.1.1

- 首个公开版本：PDH 忙率 + ETW 每进程磁盘占用统计、分级限速（优先级 / 吞吐上限 / 间歇挂起）、托盘常驻、开机自启。
